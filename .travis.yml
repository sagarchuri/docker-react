language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t sagarchuri/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true sagarchuri/docker-react npm run test